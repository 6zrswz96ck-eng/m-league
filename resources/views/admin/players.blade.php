@extends('layout')
@section('content')<p class="eyebrow">ADMIN / FALLBACK</p><h1 class="page-title">選手データの手動修正</h1><p class="page-intro">公式データを取得できないときや選手情報に変更があったときに使います。手動で直したポイントは、次の自動更新が成功すると公式の値で上書きされます。</p><section class="card"><h2>選手登録</h2><form method="post" action="{{ route('players.store') }}">@csrf<div class="formrow"><label>名前 <input name="name" required></label><label>チーム <input name="team_name" required></label><label>ポイント <input name="season_point" type="number" step="0.1" value="0" required></label><button>登録</button></div></form></section>
@foreach($players as $player)<section class="card"><form method="post" action="{{ route('players.update',$player) }}">@csrf @method('PUT')<div class="formrow"><label>名前 <input name="name" value="{{ $player->name }}" required></label><label>チーム <input name="team_name" value="{{ $player->team_name }}" required></label><label>ポイント <input name="season_point" type="number" step="0.1" value="{{ $player->season_point }}" required></label><button>保存</button></div></form></section>@endforeach
@endsection
